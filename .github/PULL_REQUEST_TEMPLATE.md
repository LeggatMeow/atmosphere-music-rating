<!-- Short description of the change -->

This PR contains the optional-login changes (allow browsing without authentication and add optional login flow).

- Ensure `.env.local` is excluded from commits and sensitive keys aren't present.
- Verify Supabase integration and optional login behavior.

/cc @LeggatMeow
